export const config = {
  defaultTimeZone: '12',
  searchDelay: 200,
  selfUrl: `${window.location.origin}/`,
  defaultCurrency: 'USD',
  defaultPhoneMask: '(000) 000-0000',
  gridOptions: {
    scrollHeight: '550px',
    limit: 25,
    rowsPerPageOptions: [25, 50, 100]
  }
};
